name: CI

on:
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Test scripts repo
        run: |
          apt-get install shellcheck
          spellcheck --version
          shellcheck source=scripts/**/*.sh
        
